{% extends 'base.html.twig' %}

{% block body %}
<nav>
    <ul>
        {% for publication in publications %}
        <li>
            <a href="{{ path('app_publication_detail', {id: publication.id}) }}">
                {{ publication.titre }}
            </a>
            <span>
                {{ publication.auteur }} - {{ publication.publieeLe | date('d/m/Y') }}
            </span>
        </li>
        {% endfor %}
    </ul>
</nav>
{% endblock %}
